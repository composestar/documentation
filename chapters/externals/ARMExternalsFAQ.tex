\para{FAQ}
\question{How do I create an external that is unique for a certain number
of filter module instances?}
\answer{If you want to use an external that is not shared with every instance of the filter module,
you can use the filter module parameters to define the different instances for the external.}